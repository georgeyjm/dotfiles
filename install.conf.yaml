- defaults:
    link:
        relink: true
        force: true
        create: true

- clean: ['~']

- link:
    ~/.profile: profile
    ~/.vimrc: vimrc
    ~/.zshrc: zshrc
    ~/.zsh_plugins: zsh_plugins

